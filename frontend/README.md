# MissionManager Frontend

Local dev:
1. cd frontend
2. npm install
3. npm run dev

API proxy: Vite proxies `/api` to `http://localhost:5000` (configured in `vite.config.ts`). Adjust if your backend runs on a different port.
